{
  "validation": {
    "rules": {
      "regionCode": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "regionCode": {
        "required": "区域编号必填",
        "minlength": "区域编号需{0}个字符以上",
        "maxlength": "区域编号需小于{0}个字符"
      },
      "name": {
        "required": "区域名称必填",
        "minlength": "区域名称需{0}个字符以上",
        "maxlength": "区域名称需小于{0}个字符"
      }
    }
  }
}