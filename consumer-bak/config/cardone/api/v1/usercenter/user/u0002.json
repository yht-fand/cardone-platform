{
  "validation": {
    "rules": {
      "userId": {
        "required": true
      },
      "name": {
        "required": true,
        "minlength": 4,
        "maxlength": 255
      }
    },
    "messages": {
      "userId": {
        "required": "用户标识必填"
      },
      "name": {
        "required": "用户名称必填",
        "minlength": "用户名称需{0}个字符以上",
        "maxlength": "用户名称需小于{0}个字符"
      }
    }
  }
}